<Window x:Class="NetworkMapViewerV2.Views.ToastNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="500" SizeToContent="Height" MinHeight="90"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        ShowActivated="False" Loaded="Window_Loaded">
    <Border x:Name="MainBorder" Background="#1E1E1E" BorderThickness="3" BorderBrush="LightGreen" CornerRadius="8" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="10" ShadowDepth="3"/>
        </Border.Effect>

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="txtIcon" Text="❌" FontSize="36" VerticalAlignment="Top" Margin="0,0,15,0" Foreground="Red"/>

            <Grid Grid.Column="1" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="txtTitle" Grid.Row="0" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,5" TextWrapping="Wrap"/>

                <ScrollViewer Grid.Row="1" MaxHeight="320" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="txtMessage" FontSize="13" Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,5,0"/>
                </ScrollViewer>
            </Grid>

            <Button Grid.Column="2" Content="✕" Width="25" Height="25" VerticalAlignment="Top" 
            Background="Transparent" Foreground="Gray" BorderThickness="0" FontSize="14"
            Click="BtnClose_Click" Cursor="Hand" ToolTip="Dismiss"/>
        </Grid>
    </Border>
</Window>