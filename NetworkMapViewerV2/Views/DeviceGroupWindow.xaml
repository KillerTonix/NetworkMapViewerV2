<Window x:Class="NetworkMapViewerV2.Views.DeviceGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Device Type" Height="450" Width="400" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Device Type Name (e.g., Firewall):" FontWeight="Bold"/>
        <TextBox x:Name="txtName" Grid.Row="1" Height="25" Margin="0,0,0,15" Padding="2"/>

        <Label Grid.Row="2" Content="Icon Image Path:" FontWeight="Bold"/>
        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtIconPath" Grid.Column="0" Height="25" Margin="0,0,10,0" Padding="2"/>
            <Button x:Name="btnBrowse" Grid.Column="1" Content="Browse..." Click="BtnBrowse_Click"/>
        </Grid>

        <Label Grid.Row="4" Content="Default Double-Click Command:" FontWeight="Bold"/>
        <ComboBox x:Name="cmbCommands" Grid.Row="5" Height="25" VerticalAlignment="Top" Margin="0,0,0,15" DisplayMemberPath="Name" SelectedValuePath="Name"/>
 
        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Height="30" Margin="0,0,10,0" IsDefault="True" Click="BtnSave_Click" />
            <Button Content="Cancel" Width="80" Height="30" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>